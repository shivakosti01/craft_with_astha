<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Successful | Craft With Astha</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Caveat:wght@700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    :root {
      --gold: #d4af37;
      --dark: #0f0f0f;
      --light-bg: #1a1a1a;
    }

    body {
      background-color: var(--dark);
      color: #fff;
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      overflow: hidden;
    }

    .success-box {
      background-color: var(--light-bg);
      border: 1px solid var(--gold);
      border-radius: 15px;
      padding: 40px 50px;
      box-shadow: 0 0 25px rgba(212, 175, 55, 0.3);
      max-width: 600px;
      position: relative;
      animation: fadeInUp 0.8s ease-in-out;
    }

    h1 {
      color: var(--gold);
      font-family: 'Caveat', cursive;
      font-size: 2.8rem;
    }

    p {
      color: #ccc;
      font-size: 1rem;
      margin-bottom: 20px;
    }

    .btn-gold {
      background-color: var(--gold);
      color: #000;
      border: none;
      border-radius: 25px;
      padding: 10px 25px;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .btn-gold:hover {
      background-color: #fff;
      color: #000;
      transform: scale(1.05);
    }

    .emoji {
      font-size: 3rem;
      margin-bottom: 15px;
      animation: bounce 1.5s infinite;
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .note {
      margin-top: 15px;
      font-size: 0.9rem;
      color: #999;
    }
  </style>
</head>
<body>

  <div class="success-box">
    <div class="emoji">🎉</div>
    <h1>Thank You, <%= name %>!</h1>
    <p>Your order has been successfully placed with <strong>Craft With Astha</strong>.</p>
    <p>We’ll send you a confirmation email soon and start preparing your beautiful handmade item 💝</p>

    <a href="/" class="btn btn-gold mt-3">← Back to Home</a>
    <p class="note">Need help or changes to your order? <br>Reach us at <a href="mailto:craftwithastha@gmail.com" style="color: var(--gold); text-decoration: none;">craftwithastha@gmail.com</a></p>
  </div>

</body>
</html>
