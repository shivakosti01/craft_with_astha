<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Place Your Order | Craft With Astha</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&family=Caveat:wght@700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root { --gold: #d4af37; --dark: #0f0f0f; --light-bg: #1a1a1a; }
    body { background-color: var(--dark); color: #fff; font-family: 'Poppins', sans-serif; padding-top: 80px; }
    .container { max-width: 600px; background: var(--light-bg); padding: 40px; border-radius: 15px; box-shadow: 0 0 20px rgba(255, 215, 0, 0.2); }
    h2 { color: var(--gold); font-family: 'Caveat', cursive; text-align: center; margin-bottom: 25px; font-size: 2.2rem; }
    input, textarea, select { width: 100%; padding: 12px; margin: 10px 0; background: transparent; border: 1px solid var(--gold); border-radius: 8px; color: #fff; outline: none; }
    input::placeholder, textarea::placeholder { color: #ccc; }
    button { background: var(--gold); color: #000; font-weight: 600; border: none; border-radius: 25px; padding: 12px 25px; width: 100%; transition: 0.3s ease; }
    button:hover { background: #fff; transform: scale(1.05); }
    label { color: var(--gold); font-weight: 500; }
    .form-note { text-align: center; font-size: 0.9rem; color: #aaa; margin-top: 10px; }
    .back-link { display: block; text-align: center; color: var(--gold); margin-top: 20px; text-decoration: none; }
    .back-link:hover { color: #fff; text-decoration: underline; }
  </style>
</head>
<body>

  <div class="container mt-5">
    <h2>🛍️ Place Your Order</h2>
    <form action="/order" method="POST">
      
      <label for="name">Full Name</label>
      <input type="text" id="name" name="name" placeholder="Enter your name" required>

      <label for="email">Email Address</label>
      <input type="email" id="email" name="email" placeholder="Enter your email" required>

      <label for="phone">Phone Number</label>
      <input type="text" id="phone" name="phone" placeholder="Enter your phone number" required>

      <label for="product">Product</label>
      <input type="text" id="product" name="product" readonly required>

      <label for="quantity">Quantity</label>
      <input type="number" id="quantity" name="quantity" placeholder="Enter quantity" min="1" required>

      <label for="address">Delivery Address</label>
      <textarea id="address" name="address" rows="3" placeholder="Enter your complete address" required></textarea>

      <label for="message">Message or Notes (Optional)</label>
      <textarea id="message" name="message" rows="3" placeholder="Add any special instructions"></textarea>

      <button type="submit">Submit Order</button>

      <p class="form-note">We’ll confirm your order via email once it’s received ✅</p>
    </form>

    <a href="/" class="back-link">← Back to Home</a>
  </div>

  <script>
    // Auto-fill product from URL
    const urlParams = new URLSearchParams(window.location.search);
    const productField = document.getElementById('product');
    productField.value = urlParams.get('product') || '';
  </script>

</body>
</html>
