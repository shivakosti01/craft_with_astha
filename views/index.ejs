<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Craft With Astha</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <!-- Bootstrap + Google Fonts -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&family=Caveat:wght@700&display=swap" rel="stylesheet">

  <style>
    /* ======== RESPONSIVE + SCROLL FIX ======== */

/* Prevent unwanted horizontal scroll */
html, body {
  overflow-x: hidden !important;
  width: 100%;
  max-width: 100%;
}

/* Make all sections fit properly */
section, footer, nav, .container {
  overflow-x: clip;
}

/* Responsive Gallery */
@media (max-width: 992px) {
  .about-img img {
    width: 100%;
  }
  .hero-content h1 {
    font-size: 2.5rem;
  }
  .hero-content p {
    font-size: 1rem;
  }
  .gallery {
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  }
  .testimonial {
    min-width: 280px;
  }
}

/* Tablet and Mobile Adjustments */
@media (max-width: 768px) {
  .navbar-brand {
    font-size: 1.6rem;
  }
  .btn-gold {
    padding: 10px 25px;
    font-size: 0.9rem;
  }
  .about-content {
    text-align: center;
  }
  .about-content h3 {
    font-size: 1.8rem;
  }
  .about-content p {
    font-size: 0.95rem;
  }
  .gallery {
    gap: 15px;
  }
  .footer-section {
    text-align: center;
  }
  .footer-social a {
    margin-right: 8px;
  }
  .newsletter-form .d-flex {
    flex-direction: column;
  }
  .newsletter-form input {
    margin-bottom: 10px;
  }
}

/* Extra small screens */
@media (max-width: 480px) {
  .hero-content h1 {
    font-size: 2rem;
  }
  .hero-content p {
    font-size: 0.9rem;
  }
  .testimonial {
    min-width: 250px;
  }
  iframe {
    height: 250px;
  }
}

    :root {
      --gold: #d4af37;
      --dark: #0f0f0f;
      --light-bg: #1a1a1a;
    }

    body {
      background-color: var(--dark);
      color: #fff;
      font-family: 'Poppins', sans-serif;
      overflow-x: hidden;
    }

    /* Navbar */
    .navbar {
      background-color: rgba(0, 0, 0, 0.9);
      border-bottom: 1px solid rgba(255, 215, 0, 0.3);
    }
    .navbar-brand {
      font-family: 'Caveat', cursive;
      color: var(--gold) !important;
      font-size: 2rem;
    }
    .nav-link { color: #fff !important; margin: 0 10px; transition: color 0.3s ease; }
    .nav-link:hover { color: var(--gold) !important; }

    /* Hero */
    .hero {
      height: 100vh;
      background: url('https://images.unsplash.com/photo-1616628188460-1f2a86559d1b?auto=format&fit=crop&w=1350&q=80') center/cover no-repeat;
      display: flex; align-items: center; justify-content: center; text-align: center;
      position: relative;
    }
    .hero::after {
      content: ""; position: absolute; top: 0; left: 0;
      width: 100%; height: 100%; background-color: rgba(0, 0, 0, 0.6);
    }
    .hero-content {
      position: relative; z-index: 2; color: var(--gold);
      max-width: 700px; padding: 20px; animation: fadeUp 1.5s ease forwards;
    }
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .hero-content h1 {
      font-family: 'Caveat', cursive;
      font-size: 4rem;
      text-shadow: 0 0 25px rgba(255,215,0,0.7);
    }
    .btn-gold  {
      background: var(--gold); color: #000;
      padding: 12px 35px; border: none; border-radius: 30px;
      font-weight: 600; margin-top: 25px;
      transition: all 0.4s ease;
    }
    .btn-gold:hover { background: #fff; transform: scale(1.05); box-shadow: 0 0 20px var(--gold); }

    section { padding: 80px 0; }
    h2.section-title {
      font-family: 'Caveat', cursive;
      color: var(--gold);
      font-size: 2.5rem;
      margin-bottom: 30px;
      text-align: center;
    }

    /* About */
    .about-img img { width:400px; border-radius: 20px; box-shadow: 0 0 20px rgba(255,215,0,0.2); }
    .about-content h3 { color: var(--gold); font-family: 'Caveat', cursive; font-size: 2rem; }
    .about-content p { color: #ccc; margin-top: 15px; line-height: 1.7; }

    /* Services */
    .service-card {
      background-color: var(--light-bg);
      border: 1px solid rgba(255,215,0,0.2);
      border-radius: 20px;
      padding: 30px; text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .service-card:hover { transform: translateY(-10px); box-shadow: 0 0 25px rgba(255,215,0,0.3); }

    /* Gallery */
    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }
    .gallery-item {
      position: relative;
      overflow: hidden;
      border-radius: 15px;
      opacity: 0;
      transform: scale(0.9);
      animation: fadeZoom 1.5s forwards;
    }
    .gallery-item:nth-child(odd) { animation-delay: 0.2s; }
    .gallery-item:nth-child(even) { animation-delay: 0.4s; }

    @keyframes fadeZoom {
      to { opacity: 1; transform: scale(1); }
    }
    .gallery-item img {
      width: 300px;
      height: 300px;
      object-fit: cover;
      border-radius: 15px;
      transition: transform 0.5s ease;
    }
    .gallery-item:hover img { transform: scale(1.1) rotate(1deg); }

    .gallery-item::after {
      content: "View Craft";
      position: absolute;
      bottom: 0; left: 0; right: 0;
      background: rgba(0, 0, 0, 0.6);
      color: var(--gold);
      text-align: center;
      padding: 10px 0;
      opacity: 0;
      transition: opacity 0.4s ease;
      font-weight: 500;
    }
    .gallery-item:hover::after { opacity: 1; }

    /* Testimonials (Auto Scroll) */
    #testimonials { overflow: hidden; position: relative; }
    .testimonial-track {
      display: flex; gap: 20px;
      animation: scrollLeft 25s linear infinite;
    }
    @keyframes scrollLeft {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }
    .testimonial {
      min-width: 350px;
      background-color: var(--light-bg);
      border-radius: 15px;
      padding: 25px;
      color: #fff;
      box-shadow: 0 0 15px rgba(255,215,0,0.1);
    }
    .testimonial p { font-style: italic; }
    .testimonial h5 { color: var(--gold); margin-top: 15px; }

    /* Contact Section */
    #contact .contact-box {
      background-color: var(--light-bg);
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 0 15px rgba(255,215,0,0.1);
    }
    form input, form textarea {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: none;
      border-radius: 10px;
      outline: none;
    }
    iframe {
      border-radius: 15px;
      width: 100%;
      height: 100%;
      min-height: 320px;
    }

    .footer-section {
  background-color: #000;
  color: #ccc;
  font-size: 0.95rem;
}

.footer-brand {
  font-family: 'Caveat', cursive;
  color: #d4af37;
  font-size: 2rem;
}

.text-gold {
  color: #d4af37 !important;
}

.footer-links li {
  margin-bottom: 8px;
}

.footer-links a {
  color: #aaa;
  text-decoration: none;
  transition: 0.3s;
}

.footer-links a:hover {
  color: #d4af37;
  padding-left: 5px;
}

.footer-social a {
  color: #d4af37;
  font-size: 1.4rem;
  margin-right: 12px;
  transition: 0.3s;
}

.footer-social a:hover {
  color: #fff;
  transform: scale(1.15);
}

.btn-gold {
  background: #d4af37;
  color: #000;
  border: none;
  border-radius: 30px;
  font-weight: 600;
  transition: all 0.3s ease;
}

.btn-gold:hover {
  background: #fff;
  transform: scale(1.05);
}

.newsletter-form input {
  border-radius: 25px;
  padding: 10px 15px;
  border: 1px solid rgba(255, 215, 0, 0.3);
  background: #111;
  color: #fff;
}

.newsletter-form input::placeholder {
  color: #aaa;
}

hr {
  border-color: rgba(255, 215, 0, 0.2);
}
.login-btn a:hover{
  color: black !important;
}





/* ///////////////////////////////////////////// */
/* ============ MOBILE FOOTER (Responsive Custom Design) ============ */
@media (max-width: 576px) {
  footer.footer-section {
    text-align: center;
    padding-top: 50px;
  }

  footer .row {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  footer .col-md-4,
  footer .col-md-2,
  footer .col-md-3 {
    width: 100%;
    margin-bottom: 25px;
  }

  footer .footer-brand {
    font-size: 1.8rem;
    margin-bottom: 10px;
  }

  footer .footer-links {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
    padding: 0;
  }

  footer .footer-links li {
    list-style: none;
    margin: 0;
  }

  footer .footer-links a {
    color: #d4af37;
    font-weight: 500;
  }

  footer .footer-social {
    margin-top: 10px;
  }

  footer .footer-social a {
    font-size: 1.5rem;
    margin: 0 10px;
  }

  footer .newsletter-form input {
    text-align: center;
    margin-bottom: 10px;
  }

  footer hr {
    display: none;
  }

  footer .text-center p {
    font-size: 0.9rem;
  }
}


/* ///////////////////////////////////////////// */


  /* ===== POPUPS ===== */
    .popup-overlay {
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0,0,0,0.8);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 9999;
    }
    .popup-box {
      background: #1a1a1a;
      border: 1px solid var(--gold);
      border-radius: 15px;
      padding: 25px 30px;
      width: 90%;
      max-width: 400px;
      color: #fff;
      box-shadow: 0 0 20px rgba(255,215,0,0.3);
      text-align: center;
      animation: popupIn 0.5s ease;
    }
    @keyframes popupIn {
      from { transform: translateY(50px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
    .popup-box h3 { color: var(--gold); margin-bottom: 15px; }
    .popup-box input, .popup-box textarea, .popup-box select {
      width: 100%; padding: 10px; margin-bottom: 10px;
      background: transparent; border: 1px solid var(--gold);
      color: #fff; border-radius: 8px;
    }
    .popup-box button {
      background: var(--gold); border: none; color: #000;
      padding: 10px 25px; border-radius: 25px; font-weight: 600;
    }
    .close-btn {
      position: absolute; top: 10px; right: 15px; color: var(--gold);
      cursor: pointer; font-size: 1.3rem;
    }
     .login-btn {
      background: var(--gold);
      color: #000;
      border-radius: 25px;
      padding: 6px 18px;
      font-weight: 600;
      border: none;
      transition: 0.3s;
    }
    .login-btn:hover {
      background: #fff;
      transform: scale(1.05);
    }
  


<style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Poppins", sans-serif;
      background: #fffaf4;
      min-height: 100vh;
    }

    /* ======== FLASH POPUP ======== */
    .flash-popup {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%) scale(0.9);
      background: transparent;
      border-radius: 18px;
      padding: 0;
      width: 600px;
      max-width: 90%;
      text-align: center;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.25);
      z-index: 9999;
      opacity: 0;
      transition: all 0.3s ease;
    }

    .flash-popup.show {
      opacity: 1;
      transform: translate(-50%, -50%) scale(1);
    }

    /* ======== BACKDROP ======== */
    .flash-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      z-index: 9998;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.3s ease;
    }

    .flash-overlay.show {
      opacity: 1;
      pointer-events: all;
    }

    /* ======== CLOSE BUTTON ======== */
    .flash-close {
      position: absolute;
      top: 8px;
      right: 12px;
      font-size: 20px;
      background: rgba(255, 255, 255, 0.8);
      border: none;
      color: #333;
      cursor: pointer;
      font-weight: bold;
      border-radius: 50%;
      width: 28px;
      height: 28px;
      line-height: 28px;
    }

    /* ======== IMAGE ======== */
    .flash-image {
      width: 600px;
      height: 400px;
      object-fit: cover;
      border-radius: 18px;
      border: 3px solid #d4af37;
      box-shadow: 0 3px 10px rgba(255, 215, 0, 0.3);
    }

    /* ======== RESPONSIVE ======== */
    @media (max-width: 650px) {
      .flash-popup {
        width: 95%;
      }
      .flash-image {
        width: 100%;
        height: auto;
      }
    }
  







</head>
<body>

<!-- ======= WhatsApp Floating Button ======= -->
<a href="https://wa.me/917000355922" class="whatsapp-float" target="_blank" aria-label="Chat on WhatsApp">
  <img src="https://cdn-icons-png.flaticon.com/512/124/124034.png" alt="WhatsApp" />
</a>

<!-- ======= Floating Call Popup (Isolated) ======= -->
<div class="cp-call-popup" id="cpCallPopup">
  <button class="cp-close-btn" id="cpCloseBtn">✕</button>
  <div class="cp-popup-content">
    <p>📞 Worried About Your Order?<br><span>Talk to an Expert Now!</span></p>
    <a href="tel:+917000355922" class="cp-call-now-btn">Call: 7000355922</a>
  </div>
</div>




<!-- Flash Overlay -->
  <div class="flash-overlay" id="flashOverlay"></div>

  <!-- Flash Image Popup -->
  <div class="flash-popup" id="flashPopup">
    <button class="flash-close" id="flashClose">&times;</button>
    <img class="flash-image" src="/images/Handmade Lamp1.png" alt="Special Offer">
  </div>

  <script>
    const flashPopup = document.getElementById("flashPopup");
    const flashOverlay = document.getElementById("flashOverlay");
    const flashClose = document.getElementById("flashClose");

    // Show popup on load
    window.addEventListener("load", () => {
      setTimeout(() => {
        flashPopup.classList.add("show");
        flashOverlay.classList.add("show");
      }, 800);
    });

    // Close when clicking X or outside
    flashClose.addEventListener("click", closePopup);
    flashOverlay.addEventListener("click", closePopup);

    function closePopup() {
      flashPopup.classList.remove("show");
      flashOverlay.classList.remove("show");
    }
  </script>





  <!-- Navbar -->
 <nav class="navbar navbar-expand-lg fixed-top">
  <div class="container">
    <a class="navbar-brand" href="#">Craft With Astha</a>
    <button class="navbar-toggler text-light" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navMenu">
      <ul class="navbar-nav">
        <li><a class="nav-link" href="/">Home</a></li>
        <li><a class="nav-link" href="/about">About</a></li>
        <li><a class="nav-link" href="/service">Services</a></li>
        <li><a class="nav-link" href="/gallery">Gallery</a></li>
        <li><a class="nav-link" href="/products">All Products</a></li>
        <li><a class="nav-link" href="/review">Reviews</a></li>
        <li><a class="nav-link" href="/contact">Contact</a></li>
      </ul>

      <% if (!currentUser) { %>
        <button class="login-btn">
          <a href="/login" style="text-decoration: none; color: white;">Login / Signup</a>
        </button>
      <% } else { %>
        <div class="text-light ms-3">
          👋 Hi, <%= currentUser.username %>!
          <a href="/logout" class="btn btn-sm btn-warning ms-2">Logout</a>
        </div>
      <% } %>
    </div>
  </div>
</nav>


 <!-- Hero -->
<!-- Hero -->
<section class="hero" id="hero" style="background: url('/images/hero.png') center/cover no-repeat;">
  <div class="hero-content">
    <h1>Handcrafted with Love & Elegance</h1>
    <p>Each creation at The Craft Studio tells a story of art, beauty, and passion.</p>
    <a href="#services" class="btn-gold">Explore Our Work</a>
  </div>
</section>





  <!-- About -->
  <section id="about">
    <div class="container">
      <div class="row g-5 align-items-center">
        <div class="col-md-6 about-img">
          <img src="/images/jasmin-schreiber-JE4iHJE-knI-unsplash.jpg" alt="Craft Studio">
        </div>
        <div class="col-md-6 about-content">
          <h3>Where Creativity Meets Craftsmanship</h3>
          <p>At The Craft Studio, every product is a reflection of passion and patience. From intricate handmade décor to personalized gifts, we design each creation to bring warmth, beauty, and joy to your space.</p>
          <p>We believe that art has the power to tell stories — and your story deserves a masterpiece.</p>
          <a href="/about" class="btn-gold">Explore More</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Services -->
  <section id="services" class="text-center">
    <h2 class="section-title">Our Services</h2>
    <div class="container">
      <div class="row g-4">
        <div class="col-md-4"><div class="service-card"><h4>Custom Gift Boxes</h4><p>Beautiful handcrafted gift hampers for special occasions.</p></div></div>
        <div class="col-md-4"><div class="service-card"><h4>Home Decor Art</h4><p>Elegant wall frames, resin tables, and decor crafts.</p></div></div>
        <div class="col-md-4"><div class="service-card"><h4>Personalized Orders</h4><p>Tailored designs that perfectly match your style.</p></div></div>
      </div>
    </div>
  </section>

  <!-- Gallery (Animated) -->
  <section id="gallery" class="text-center">
    <h2 class="section-title">Gallery</h2>
    <div class="container gallery">
      <a href="/products"><div class="gallery-item"><img src="/images/pppp.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item" ><img src="/images/lamp.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/giftbox.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/name chain.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/mini frame.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/pen.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/modern frame.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/lamp1.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/lamp2.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/handmade wallet.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/phone cover.png" alt=""></div></a>
      <a href="/products"><div class="gallery-item"><img src="/images/girls.png" alt=""></div></a>
    </div>
  </section>

  <!-- Testimonials -->
  <section id="testimonials" class="text-center">
    <h2 class="section-title">Customer Reviews</h2>
    <div class="testimonial-track">
      <div class="testimonial"><p>“Absolutely stunning craftsmanship! I ordered a handmade wall art and it exceeded my expectations.”</p><h5>- Neha Sharma</h5></div>
      <div class="testimonial"><p>“Every product from The Craft Studio feels so personal and elegant. Highly recommended!”</p><h5>- Riya Gupta</h5></div>
      <div class="testimonial"><p>“The detailing is just amazing! Perfect gifts for loved ones. I’m definitely ordering again.”</p><h5>- Meera Jain</h5></div>
      <div class="testimonial"><p>“Loved the resin art frame. Delivered on time and beautifully packed.”</p><h5>- Ananya Verma</h5></div>
      <div class="testimonial"><p>“Crafts that truly speak from the heart. Best studio in town!”</p><h5>- Sakshi Patel</h5></div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" class="text-center">
    <h2 class="section-title">Contact Us</h2>
    <div class="container">
      <div class="row g-4 align-items-stretch">
        <div class="col-md-6">
          <div class="contact-box h-100">
            <form>
              <input type="text" placeholder="Your Name" required>
              <input type="email" placeholder="Your Email" required>
              <textarea rows="5" placeholder="Your Message" required></textarea>
              <button class="btn-gold w-100">Send Message</button>
            </form>
          </div>
        </div>
        <div class="col-md-6">
          <iframe src="https://www.google.com/maps?q=indore&output=embed" loading="lazy"></iframe>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <!-- ====== Footer Section ====== -->
<footer class="footer-section text-light pt-5 pb-4">
  <div class="container">
    <div class="row gy-4">
      
      <!-- Brand + About -->
      <div class="col-md-4">
        <h3 class="footer-brand mb-3">Craft With Astha</h3>
        <p class="footer-text">
          Where creativity meets craftsmanship. Handcrafted décor, personalized gifts, and art that speaks your story.
        </p>
      </div>

      <!-- Quick Links -->
      <div class="col-md-2">
        <h5 class="text-gold mb-3">Quick Links</h5>
        <ul class="list-unstyled footer-links">
          <li><a href="/">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/service">Services</a></li>
          <li><a href="/gallery">Gallery</a></li>
          <li><a href="#testimonials">Reviews</a></li>
          <li><a href="#blog">Blog</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </div>

      <!-- Contact Info -->
      <div class="col-md-3">
        <h5 class="text-gold mb-3">Contact Us</h5>
        <p class="mb-1"><i class="bi bi-geo-alt-fill text-gold me-2"></i>Indore, Madhya Pradesh, India</p>
        <p class="mb-1"><i class="bi bi-telephone-fill text-gold me-2"></i>+91 98765 43210</p>
        <p><i class="bi bi-envelope-fill text-gold me-2"></i>asthajain0804@gmail.com</p>

        <div class="footer-social mt-3">
          <a href="https://www.instagram.com/craft_with_astha?utm_source=ig_web_button_share_sheet&igsh=ZDNlZDc0MzIxNw=="><i class="bi bi-instagram"></i></a>
          <a href="#"><i class="bi bi-facebook"></i></a>
          <a href="https://wa.me/qr/JHNNYTHLQTJIJ1"><i class="bi bi-whatsapp"></i></a>
          <a href="#"><i class="bi bi-envelope"></i></a>
        </div>
      </div>

      <!-- Newsletter -->
      <div class="col-md-3">
        <h5 class="text-gold mb-3">Stay Updated</h5>
        <p class="footer-text">Get our latest craft ideas, offers, and updates straight to your inbox.</p>
        <form class="newsletter-form">
          <input type="email" class="form-control mb-2" placeholder="Enter your email">
          <button class="btn btn-gold w-100">Subscribe</button>
        </form>
      </div>

    </div>

    <hr class="mt-5 mb-3 text-gold">

    <div class="text-center">
      <p class="mb-0">© 2025 <span class="text-gold">Craft With Astha</span> | Crafted with ❤️</p>
    </div>
  </div>
</footer>


 <!-- ==================== POPUPS ==================== -->

  <!-- Login -->
  <!-- <div class="popup-overlay" id="loginPopup">
    <div class="popup-box position-relative">
      <span class="close-btn" onclick="closePopup('loginPopup')">&times;</span>
      <h3>Login</h3>
      <input type="text" placeholder="Email or Username">
      <input type="password" placeholder="Password">

      <button onclick="loginSuccess()">Login</button>
      
        <button class="login-btn" data-bs-toggle="modal" data-bs-target="#authModal" id="signupBtn"> Signup</button>
    </div>
  </div> -->

  <!-- Signup -->
  <!-- <div class="popup-overlay" id="signupPopup">
    <div class="popup-box position-relative">
      <span class="close-btn" onclick="closePopup('signupPopup')">&times;</span>
      <h3>Signup</h3>
      <input type="text" placeholder="Full Name">
      <input type="email" placeholder="Email">
      <input type="password" placeholder="Password">
      <button onclick="signupSuccess()">Signup</button>
    </div>
  </div> -->

  <!-- Order Form -->
  <!-- <div class="popup-overlay" id="orderPopup">
    <div class="popup-box position-relative">
      <span class="close-btn" onclick="closePopup('orderPopup')">&times;</span>
      <h3>Order Now</h3>
      <input type="text" placeholder="Full Name">
      <input type="text" placeholder="Product Name">
      <input type="number" placeholder="Quantity">
      <textarea placeholder="Delivery Address"></textarea>
      <button>Place Order</button>
    </div>
  </div> -->

  <!-- Scripts -->
  <!-- <script>
    const loginBtn = document.getElementById('loginBtn');
    const signupBtn = document.getElementById('signupBtn');

    function openPopup(id) {
      document.getElementById(id).style.display = 'flex';
    }

    function closePopup(id) {
      document.getElementById(id).style.display = 'none';
    }

    loginBtn.addEventListener('click', () => openPopup('loginPopup'));
    signupBtn.addEventListener('click', () => openPopup('signupPopup'));

    function loginSuccess() {
      closePopup('loginPopup');
      setTimeout(() => openPopup('orderPopup'), 10000); // 10 sec after login
    }

    function signupSuccess() {
      closePopup('signupPopup');
      setTimeout(() => openPopup('orderPopup'), 10000);
    }
  </script> -->




<!-- Bootstrap Icons -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
<script src="/javascripts/script.js"></script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
